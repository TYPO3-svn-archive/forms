;<?php die() ?>;

; normal input tag for text
input = "<input %s />"

; textarea tag
textarea = "<textarea %s>%s</textarea>"

; select tag begin
selectbegin = "<select %s>"

; select tag end
selectend = "</select>"

; option tag
option = "<option value="%s" %s>%s</option>"

; checkbox tag
checkbox = "<input type="checkbox" %s />"

; radio button tag
radio = "<input type="radio" %s />"

; password tag
password = "<input type="password" %s />"

; hidden tag
hidden = "<input type="hidden" %s />"

; general tag, not self enclosed
tag = "<%1$s %2$s>%3$s</%1$s>"

; general tag, self enclosed
tagse = "<%s %s />"